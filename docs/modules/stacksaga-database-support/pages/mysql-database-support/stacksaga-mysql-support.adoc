:impl_name: mysql
:title: Stacksaga MySQL Reactive Support
:description: Learn how to integrate Stacksaga with MySQL using the reactive support module. Step-by-step guide for schema setup, connection configuration, and transaction retry in distributed systems.
:doctitle: Stacksaga MySQL Reactive Support
:revnumber: 1.0.0
:revdate: {localdate}
:keywords: Stacksaga, MySQL, Reactive Support, Database Integration, Event Store, Transaction Retry, Distributed Systems, Java, Orchestrator, Schema Creation, Connection Configuration, R2DBC, High Throughput, Idempotency, Saga design pattern, Spring Boot, Spring WebFlux, Spring Saga
:robots: index,follow

= Stacksaga Mysql Reactive Support

== Overview

`stacksaga-mysql-reactive-support` is the *mysql reactive(non-blocking)* implementation of the xref:stacksaga-database-support:overview/database-support-overview.adoc[].
it provides all the necessary facilities for accessing the mysql database for the Stacksaga engine. and also it provides the endpoints for accessing the event-store to see the tracing details from the xref:trace-window:stacksaga-trace-window.adoc[].

Here you can see where the `stacksaga-mysql-reactive-support` fits into the overall Stacksaga architecture.

image::mysql/stacksaga-diagram-stacksaga-components-database-support-mysql.svg[alt="stacksaga components overview with mysql database support",title="Stacksaga components overview with mysql database support"]

NOTE: read xref:stacksaga-database-support:overview/database-support-overview.adoc[] to have a better understanding of the database support modules like the responsibilities, architecture etc.

[[adding-mysql-support]]
=== Adding `stacksaga-mysql-reactive-support` as a dependency

Here is the way that you can add the library into your existing orchestrator application as a dependency.

.Adding `stacksaga-mysql-reactive-support` as a dependency
[source,xml]
----
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.stacksaga</groupId>
            <artifactId>stacksaga-bom</artifactId>
            <version>1.1.0</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
<dependency>
    <groupId>org.stacksaga</groupId>
    <artifactId>stacksaga-mysql-reactive-support</artifactId>
</dependency>
----

[[configuration-properties]]
== Configuration Properties

[.scrollable-div]
--

[cols="1,1,1,<1"]
[.properties-table]
|===
|Property Name|Default Value|Type|Description

4+| you can use the default spring `r2dbc` mysql connection properties that starts with `spring.r2dbc` prefix to configure the mysql connection for the stacksaga engine. otherwise, you can provide a custom one as mentioned xref:#connection-configuration[here].

|`stacksaga.mysql.transaction.retry.crashed-restore-retention-period-seconds` | `300` (five-minutes) | long | if the transaction has not been updated after configured duration, it is considered as the transaction was dismissed due to crash, or it may be in a queue in rare case. even if the transaction is in a queue, the transaction is restored for retrying by considering it has crashed.
the value should be provided in seconds.

|`stacksaga.mysql.transaction.retry.retention-period-seconds` | `60` (one-minute) | long | How long the transaction should be kept waiting to next expose the transaction for retrying.
This ensures that avoiding the transaction retrying frequently withing a short period of time. the transaction is frozen for some period of time, even the transaction failed again after retrying recently.

|`stacksaga.mysql.enabled`|`true`|`boolean`| Enable or disable the starter entirely. Defaults to true so it activates automatically when added.

4+|*The following `stacksaga.mysql.jdbc.*` properties are used by Liquibase for database migration and schema creation.*

|`stacksaga.mysql.jdbc.url`|`-`|`String`| The JDBC URL used by Liquibase for database migration and schema creation.
|`stacksaga.mysql.jdbc.username`|`-`|`String`| The username for the JDBC connection used by Liquibase.
|`stacksaga.mysql.jdbc.password`|`-`|`String`| The password for the JDBC connection used by Liquibase.
|`stacksaga.mysql.jdbc.database`|`-`|`String`| The database name to connect to via JDBC for Liquibase migrations.
|`stacksaga.mysql.jdbc.properties`|`-`|`Map<String,String>`| Additional JDBC driver properties as key-value pairs, passed to the Liquibase connection.

4+|*The following `stacksaga.mysql.r2dbc.*` properties are used for the regular reactive event-store actions (read/write operations against the event store).*

|`stacksaga.mysql.r2dbc.url`|`-`|`String`| The R2DBC connection URL used by the event-store. If set, takes precedence over the individual host/port/database settings.
|`stacksaga.mysql.r2dbc.host`|`localhost`|`String`| The MySQL server host for the reactive event-store connection.
|`stacksaga.mysql.r2dbc.port`|`3306`|`int`| The MySQL server port for the reactive event-store connection.
|`stacksaga.mysql.r2dbc.database`|`-`|`String`| The database name used by the event-store via R2DBC.
|`stacksaga.mysql.r2dbc.username`|`-`|`String`| The username for the event-store's R2DBC connection.
|`stacksaga.mysql.r2dbc.password`|`-`|`String`| The password for the event-store's R2DBC connection.
|`stacksaga.mysql.r2dbc.unix-domain-socket-path`|`-`|`String`| The path to a Unix domain socket to use instead of a TCP host/port connection for the event-store.
|`stacksaga.mysql.r2dbc.connect-timeout`|`10s`|`Duration`| The maximum time to wait while establishing an event-store connection to the MySQL server.
|`stacksaga.mysql.r2dbc.socket-timeout`|`30s`|`Duration`| The maximum time to wait for socket I/O operations on the event-store connection.
|`stacksaga.mysql.r2dbc.server-zone-id`|`UTC`|`String`| The time zone ID assumed for the MySQL server when converting temporal values in the event-store.
|`stacksaga.mysql.r2dbc.use-server-prepare-statement`|`false`|`boolean`| Whether the event-store uses server-side prepared statements instead of client-side (text) statements.
|`stacksaga.mysql.r2dbc.zero-date-option`|`USE_NULL`|`String`| How to handle MySQL "zero date" values (`0000-00-00`) in the event-store. One of `USE_NULL`, `USE_ROUND`, `EXCEPTION`.
|`stacksaga.mysql.r2dbc.tcp-keep-alive`|`false`|`boolean`| Whether to enable TCP keep-alive on the event-store's underlying socket connection.
|`stacksaga.mysql.r2dbc.tcp-no-delay`|`true`|`boolean`| Whether to enable TCP no-delay (disables Nagle's algorithm) on the event-store's underlying socket connection.
|`stacksaga.mysql.r2dbc.autodetect-extensions`|`true`|`boolean`| Whether to autodetect and load available R2DBC driver extensions for the event-store.
|`stacksaga.mysql.r2dbc.options`|`-`|`Map<String,String>`| Additional R2DBC driver-specific connection options for the event-store, as key-value pairs.
|`stacksaga.mysql.r2dbc.ssl.mode`|`PREFERRED`|`SslMode`| The SSL mode to use for the event-store connection (e.g. `DISABLED`, `PREFERRED`, `REQUIRED`, `VERIFY_CA`, `VERIFY_IDENTITY`).
|`stacksaga.mysql.r2dbc.ssl.ca`|`-`|`String`| Path to the CA certificate used to verify the event-store server's certificate.
|`stacksaga.mysql.r2dbc.ssl.cert`|`-`|`String`| Path to the client certificate used for mutual TLS authentication with the event-store.
|`stacksaga.mysql.r2dbc.ssl.key`|`-`|`String`| Path to the client private key used for mutual TLS authentication with the event-store.
|`stacksaga.mysql.r2dbc.ssl.key-password`|`-`|`String`| The password protecting the client private key, if encrypted.
|`stacksaga.mysql.r2dbc.ssl.tls-version`|`-`|`String`| The TLS protocol version(s) to allow for the event-store connection, e.g. `TLSv1.2,TLSv1.3`.
|`stacksaga.mysql.r2dbc.pool.initial-size`|`5`|`int`| The number of connections to create in the event-store's connection pool on startup.
|`stacksaga.mysql.r2dbc.pool.max-size`|`20`|`int`| The maximum number of connections allowed in the event-store's connection pool.
|`stacksaga.mysql.r2dbc.pool.max-idle-time`|`30m`|`Duration`| The maximum time a connection may remain idle in the event-store's pool before being closed.
|`stacksaga.mysql.r2dbc.pool.max-life-time`|`1h`|`Duration`| The maximum lifetime of a connection in the event-store's pool before it is closed and replaced.
|`stacksaga.mysql.r2dbc.pool.max-acquire-time`|`10s`|`Duration`| The maximum time to wait when acquiring a connection from the event-store's pool before failing.
|`stacksaga.mysql.r2dbc.pool.validation-query`|`SELECT 1`|`String`| The query used to validate an event-store connection before it is handed out from the pool.
|===

--
